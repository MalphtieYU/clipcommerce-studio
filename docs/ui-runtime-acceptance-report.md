# 桌面端运行验收记录

验收日期：2026-07-18
地址：http://127.0.0.1:5173
方式：Browser 插件不可用，使用 Playwright + Microsoft Edge 回退验证。

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 页面不为空白 | 通过 | 1440×900 与 1280×800 均有标题、导航、筛选和内容区 |
| 页面身份与路由 | 通过 | 8 个核心页面及单条素材分析台均可通过 hash 路由刷新恢复 |
| 框架错误覆盖层 | 通过 | 无 Vite/React 错误覆盖层 |
| 控制台健康 | 通过 | 两种视口的页面巡检均无 warning、error、pageerror |
| 横向溢出 | 通过 | 20 个页面状态记录均为 horizontalOverflow: false |
| 顶部栏与侧边栏 | 通过 | 本机 SQLite、匿名演示标识、全局搜索入口与导航可见 |
| 工作驾驶舱 | 通过 | KPI、今日重点、高潜素材、日常观察表已呈现 |
| 产品/素材/目标操作 | 通过 | 新增、版本、进度写入后刷新仍保留 |
| CSV 导入 | 通过 | 真实执行解析、校验、写入，且公式注入被拒绝 |
| XLSX 导入 | 通过 | 三份标准模板均执行解析、校验、写入、撤销 |

修复说明：空白页问题由启动地址不一致和统一启动器未等待 API 就绪共同造成。现在统一使用 npm run dev，前端地址为 5173，API 只绑定 127.0.0.1:8787；启动后已进行健康检查和全页截图回归。

已知边界：本期是桌面优先的内部本机工作站，未以移动端为验收目标；周会报告、素材复盘结论与导出等未接入功能保持显式禁用。
